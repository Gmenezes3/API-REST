class Pedido {
    constructor(id, id_cliente, valor, itens ){
        this.id = id,
        this.id_cliente = id_cliente,
        this.itens = itens,
        this.valor = valor
    }
}

export default Pedido;