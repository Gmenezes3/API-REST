class Unidade{
    constructor(id, endereco, cnpj, telefone, id_unidade){
        this.id = id,
        this.unidade = unidade,
        this.endereco = endereco,
        this.cnpj = cnpj,
        this.telefone = telefone
    }
}

export default Unidade;